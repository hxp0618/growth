<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.growth.dao.FamilyRoleMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.growth.entity.FamilyRole">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_code" property="roleCode" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="permissions" property="permissions" jdbcType="VARCHAR" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="BIGINT"/>
        <result column="update_by" property="updateBy" jdbcType="BIGINT"/>
        <result column="is_deleted" property="isDeleted" jdbcType="TINYINT"/>
        <result column="version" property="version" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 根据状态查询角色列表 -->
    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM family_roles 
        WHERE is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC, create_time ASC
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" resultMap="BaseResultMap">
        SELECT * FROM family_roles 
        WHERE role_code = #{roleCode} 
        AND is_deleted = 0
        LIMIT 1
    </select>

    <!-- 查询启用状态的角色列表（按排序序号排序） -->
    <select id="selectEnabledRolesOrderBySortOrder" resultMap="BaseResultMap">
        SELECT * FROM family_roles 
        WHERE status = 1 
        AND is_deleted = 0
        ORDER BY sort_order ASC, create_time ASC
    </select>

</mapper>